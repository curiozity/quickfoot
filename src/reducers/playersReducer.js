import { types } from "../types/types";

const initialState = {
    nombre: '',
    demarcacion: ''
}

export const playersReducer = ( state = initialState , action ) => {

    switch ( action.type ) {
        
    
    
        default:
            return state;
    }

}